server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    client_max_body_size 25m;

    location = /healthz {
        access_log off;
        return 200 "ok\n";
    }

    location = /laopos {
        return 301 /laopos/;
    }

    location = /laopos/index.html {
        add_header Cache-Control "no-store" always;
        try_files /laopos/index.html =404;
    }

    location /laopos/ {
        add_header Cache-Control "no-store" always;
        try_files $uri $uri/ /laopos/index.html;
    }

    location ~* ^/laopos/.+\.(?:js|css|woff2?|ttf|eot|svg|png|jpg|jpeg|gif|ico)$ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=2592000, immutable" always;
    }
}
